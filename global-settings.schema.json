{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "GlobalSettings",
  "description": "Configurazione centralizzata del progetto. Letto direttamente da scripts/deploy.sh (bash), dal backend ASP.NET Core (AddJsonFile) e dal frontend Node SSR (readFileSync). Modificare questo file e rieseguire il deploy è sufficiente per applicare la configurazione a tutti i livelli.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "_nota": {
      "type": "string",
      "description": "Nota descrittiva libera (ignorata dal runtime). Usata in testa ai file per ricordare a cosa serve ciascuno."
    },
    "project": {
      "type": "object",
      "description": "Identità dell'app — fonte unica. Nome e versione vivono SOLO qui: scripts/deploy.sh li slugifica per il nome progetto Docker e li passa come build ARG; il frontend li riceve iniettati in environment.ts (navbar, titolo, manifest PWA, meta app-version, rilevamento aggiornamenti).",
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Nome visualizzato dell'app. Mostrato in navbar/titolo/manifest e slugificato per COMPOSE_PROJECT_NAME (es. 'Mercatino App' → 'mercatino-app').",
          "examples": ["App", "Mercatino App", "MioProgetto"]
        },
        "version": {
          "type": "string",
          "description": "Versione dell'app (es. '1.2.0'). Propagata nel meta app-version e nel manifest; usata da VersionCheckService per rilevare gli aggiornamenti.",
          "default": "1.0.0",
          "examples": ["1.0.0", "2.3.1"]
        },
        "lastModified": {
          "type": "string",
          "pattern": "^\\d{2}/\\d{2}/\\d{4}$",
          "description": "Data di ultima modifica dei contenuti, formato italiano GG/MM/AAAA. Alimenta il <lastmod> della sitemap e og:updated_time. Bumpala a mano quando i contenuti cambiano davvero. Se assente/non valida, il build usa la data odierna.",
          "examples": ["08/06/2026"]
        }
      }
    },
    "frontend": {
      "type": "object",
      "description": "Configurazione del container frontend (Angular SSR + Node). Letta da scripts/deploy.sh per le variabili docker-compose.",
      "additionalProperties": false,
      "properties": {
        "hostname": {
          "type": "string",
          "description": "Dominio pubblico del sito senza schema (es. 'miodominio.it'). Usato per FRONTEND_BASE_URL, NG_ALLOWED_HOSTS e per derivare Security.CorsOrigins. Se vuoto, il frontend Node SSR accetta solo gli host locali (localhost, 127.0.0.1, [::1]): fail-closed, così in produzione senza hostname il traffico reale viene rifiutato (421) invece di accettare qualsiasi Host. Valorizzarlo (o impostare NG_ALLOWED_HOSTS) per servire il dominio reale.",
          "examples": ["", "miodominio.it", "www.miodominio.it"]
        },
        "port": {
          "type": "integer",
          "description": "Porta esposta dal container frontend sull'host Docker. La porta interna del container Node SSR è sempre 3000; questa è la porta del mapping host:container.",
          "default": 3000,
          "minimum": 1,
          "maximum": 65535
        }
      }
    },
    "backend": {
      "type": "object",
      "description": "Configurazione dell'esposizione del container backend. Il backend è normalmente interno alla rete Docker; abilitare 'public' solo per API pubbliche o deploy separato.",
      "additionalProperties": false,
      "properties": {
        "public": {
          "type": "boolean",
          "description": "Se true, il backend viene esposto su una porta dell'host (richiede docker-compose.backend-exposed.yml). Default: false.",
          "default": false
        },
        "publicPort": {
          "type": ["integer", "null"],
          "description": "Porta host su cui esporre il backend (usata solo se 'public: true').",
          "examples": [null, 8080]
        }
      }
    },
    "Localization": {
      "type": "object",
      "description": "Lingue del sito come codici a due lettere: SORGENTE UNICA, consumata in modo indipendente da entrambe le parti — il backend le arricchisce nelle culture .NET tipizzate (RequestLocalization, messaggi d'errore localizzati), il frontend deriva cultura e formattazione (BCP-47, nomi nativi, giorni) via Intl. Lette anche a build/module-load (routing per-lingua, fallback di pickLocaleText).",
      "additionalProperties": false,
      "properties": {
        "DefaultLanguage": {
          "type": "string",
          "description": "Lingua di default (codice ISO 639-1, es. 'it'). Deve essere inclusa in SupportedLanguages.",
          "examples": ["it", "en", "de", "fr"]
        },
        "SupportedLanguages": {
          "type": "array",
          "description": "Lingue supportate come codici a due lettere. Deve includere DefaultLanguage. Per ognuna serve il catalogo i18n (basic/addon) e i file legali.",
          "items": { "type": "string", "examples": ["it", "en", "de", "fr"] },
          "minItems": 1,
          "examples": [["it"], ["it", "en"]]
        }
      }
    },
    "Security": {
      "type": "object",
      "description": "Sicurezza specifica del progetto: chiavi API, origini CORS, reverse proxy e token JWT. Gli header di sicurezza fissi (uguali per ogni progetto) vivono in security-headers.json, file del template che il progetto figlio non gestisce.",
      "additionalProperties": false,
      "properties": {
        "ApiConfig": {
          "type": "object",
          "description": "Configurazione delle API: chiavi accettate e rate limiting.",
          "additionalProperties": false,
          "properties": {
            "Keys": {
              "type": "array",
              "description": "Chiavi API accettate dal backend per le chiamate interne (header X-Api-Key). Il frontend Node SSR usa Security.ApiConfig.Keys[0] per autenticarsi al backend. In produzione usare una stringa casuale di almeno 32 caratteri.",
              "items": { "type": "string" },
              "minItems": 1,
              "examples": [["frontend"], ["supersecretkey32characters_minimum"]]
            },
            "RateLimiting": {
              "type": "object",
              "description": "Rate limiting delle API: soglia globale (per IP) e soglia dedicata al login (POST /auth/login).",
              "additionalProperties": false,
              "properties": {
                "Enabled": {
                  "type": "boolean",
                  "description": "Attiva/disattiva l'enforcement. Se false, le policy restano registrate ma non contano nulla — utile dietro un WAF/reverse proxy che applica già le proprie soglie.",
                  "default": true
                },
                "Global": {
                  "type": "object",
                  "description": "Limite globale, partizionato per IP client.",
                  "additionalProperties": false,
                  "properties": {
                    "PermitLimit": { "type": "integer", "description": "Richieste ammesse per finestra.", "default": 500, "minimum": 1 },
                    "WindowSeconds": { "type": "integer", "description": "Durata della finestra, in secondi.", "default": 60, "minimum": 1 }
                  }
                },
                "Login": {
                  "type": "object",
                  "description": "Limite su POST /auth/login, partizionato per IP client — pensato per rendere impraticabile il brute force sulle credenziali.",
                  "additionalProperties": false,
                  "properties": {
                    "PermitLimit": { "type": "integer", "description": "Richieste ammesse per finestra.", "default": 5, "minimum": 1 },
                    "WindowSeconds": { "type": "integer", "description": "Durata della finestra, in secondi.", "default": 60, "minimum": 1 }
                  }
                }
              }
            }
          }
        },
        "CorsOrigins": {
          "type": "array",
          "description": "Origini CORS ammesse per il backend. In produzione impostare l'URL completo con schema (es. 'https://miodominio.it'). Lasciare vuoto in sviluppo locale (il backend è interno alla rete Docker).",
          "items": { "type": "string" },
          "examples": [[], ["https://miodominio.it"]]
        },
        "BehindProxy": {
          "type": "boolean",
          "description": "Indica se il backend è dietro un reverse proxy (es. nginx, Cloudflare). Se true, il backend legge l'IP reale da X-Forwarded-For. Impostare a true quando si usa frontend.hostname.",
          "default": false
        },
        "Token": {
          "type": "object",
          "description": "Configurazione JWT per il sistema di login. Il login si attiva automaticamente quando SecretKey è valorizzata (>=32 caratteri); lasciare SecretKey vuota per disabilitarlo.",
          "additionalProperties": false,
          "properties": {
            "SecretKey": {
              "type": "string",
              "description": "Chiave segreta per firmare i JWT. Deve essere lunga almeno 32 caratteri in produzione. Lasciare vuota per disabilitare il login.",
              "examples": ["", "supersecretjwtkeyatleast32characters"]
            },
            "ExpirationSeconds": {
              "type": "integer",
              "description": "Durata in secondi dei token JWT emessi. Default: 3000 (50 minuti).",
              "default": 3000,
              "minimum": 60
            }
          }
        },
        "CryptoSecret": {
          "type": "string",
          "description": "Chiave segreta per il servizio di cifratura generico dell'engine (EngineCrypto, AES-256-GCM) — usata ad es. dall'export dati personali (GET /me/data). Volutamente separata da Token.SecretKey: non va derivata da essa né riusata altrove. setup.mjs la genera già alla nascita del progetto, indipendentemente dal login. Rigenerarla con: openssl rand -base64 32.",
          "examples": ["", "supersecretcryptokeyatleast32characters"]
        }
      }
    },
    "Mail": {
      "type": "object",
      "description": "Configurazione SMTP del mailer dell'engine (EngineMailer). Contiene SEGRETI (password) → vive in global-settings.local.json. Si attiva come il login: senza Host e FromAddress il mailer resta spento e ogni invio risponde 503. Volutamente generica (SMTP standard): lo stesso codice spedisce con OVH, Brevo, Mailgun, Amazon SES, Gmail o un relay locale cambiando solo questi valori. Per non finire in spam, FromAddress deve stare sul tuo dominio e vanno configurati i record DNS SPF/DKIM/DMARC.",
      "additionalProperties": false,
      "properties": {
        "Host": {
          "type": "string",
          "description": "Host del server SMTP. Es. OVH: 'ssl0.ovh.net'.",
          "examples": ["ssl0.ovh.net", "smtp-relay.brevo.com", "email-smtp.eu-west-1.amazonaws.com"]
        },
        "Port": {
          "type": "integer",
          "description": "Porta SMTP. 587 (STARTTLS) o 465 (SSL/TLS) nella maggior parte dei casi.",
          "default": 587,
          "minimum": 1,
          "maximum": 65535
        },
        "Security": {
          "type": "string",
          "description": "Modalità di cifratura del canale (TLS sempre obbligatorio). 'Auto' sceglie in sicurezza dalla porta: 465 -> SslOnConnect, altre -> StartTls (mai connessione in chiaro). 'StartTls' tipico su 587; 'SslOnConnect' tipico su 465; 'None' SOLO per relay locali fidati.",
          "enum": ["Auto", "None", "StartTls", "SslOnConnect"],
          "default": "Auto"
        },
        "Username": {
          "type": "string",
          "description": "Utente SMTP (di solito l'indirizzo completo della casella). Vuoto = invio senza autenticazione (relay aperti/locali).",
          "examples": ["", "noreply@miodominio.it"]
        },
        "Password": {
          "type": "string",
          "description": "Password SMTP della casella. SEGRETO: tienila solo in global-settings.local.json."
        },
        "FromAddress": {
          "type": "string",
          "description": "Mittente di default (usato quando il chiamante non passa un 'from'). DEVE stare sul tuo dominio: è ciò che fa quadrare SPF/DKIM. Mai l'indirizzo di un visitatore.",
          "examples": ["", "noreply@miodominio.it"]
        },
        "FromName": {
          "type": "string",
          "description": "Nome visualizzato di default del mittente (opzionale).",
          "examples": ["", "Sito MioDominio"]
        },
        "TimeoutSeconds": {
          "type": "integer",
          "description": "Timeout di connessione/invio in secondi.",
          "default": 30,
          "minimum": 1
        },
        "MaxAttachmentBytes": {
          "type": "integer",
          "description": "Dimensione massima totale degli allegati per messaggio, in byte. Superata -> errore 413. Default 10485760 (10 MB). 0 = nessun limite (sconsigliato su endpoint pubblici).",
          "default": 10485760,
          "minimum": 0
        },
        "VerifyRecipientDomain": {
          "type": "boolean",
          "description": "Se true, prima di inviare verifica via DNS che il dominio dei destinatari possa ricevere posta (record MX, fallback A/AAAA): typo di dominio (gmail.con) e domini inesistenti vengono scartati con 400 senza aprire l'SMTP. NON verifica l'esistenza della casella. Default false: nessuna query DNS se non lo accendi.",
          "default": false
        }
      }
    },
    "ErrorReporting": {
      "type": "object",
      "description": "Segnalazione errori dell'engine (IErrorReportingService): un POST JSON verso un webhook esterno per ogni eccezione non applicativa o applicativa con status >=500. Non contiene segreti in senso stretto ma vive comunque in global-settings.local.json perché è pubblicazione/ambiente, non identità del progetto. Volutamente senza SDK di terze parti: un webhook HTTP generico, non l'endpoint proprietario di un vendor. Spento di default (WebhookUrl vuoto).",
      "additionalProperties": false,
      "properties": {
        "WebhookUrl": {
          "type": "string",
          "description": "URL del webhook a cui inviare la segnalazione (POST JSON: message, exceptionType, statusCode, path, method, stackTrace troncato, timestamp). Vuoto = spento, nessuna chiamata uscente.",
          "examples": ["", "https://esempio.tld/webhook/errori"]
        }
      }
    },
    "site": {
      "type": "object",
      "description": "Identità ed estetica del sito (committabile, del progetto). Iniettata nel frontend al build via environment.ts. I flag di comportamento (showNav, showFooter, showPanel, fixedTopHeader, showLoginInHeader, showNotifications, panelSurface, forceThemeTone, designSystem, isWebApp, onlyPlainImage) sono struttura e vivono in site.ts (`shell` / `isWebApp` / `onlyPlainImage`), insieme a pagine e loginPage; il menu di header/footer e l'icona di brand sono dato risolto a runtime in nav.ts.",
      "additionalProperties": false,
      "properties": {
        "description": {
          "type": "object",
          "description": "Descrizione generale del sito, per-lingua: chiavi = tag lingua (come Localization.SupportedLanguages), valori = testo. Usata in meta description SEO, footer e JSON-LD; il build usa la lingua default per i file statici, l'SSR localizza per richiesta. Accettata anche come singola stringa (legacy), normalizzata sulla lingua default.",
          "additionalProperties": { "type": "string" },
          "examples": [{ "it": "Descrizione del sito", "en": "Site description" }]
        },
        "colorTema": {
          "type": "string",
          "pattern": "^#([0-9a-fA-F]{3}|[0-9a-fA-F]{6})$",
          "description": "Colore tema principale (hex #RGB o #RRGGBB). Genera la palette OKLCH e il tono testo chiaro/scuro.",
          "examples": ["#131e55", "#0d6efd"]
        },
        "colorSecondary": {
          "type": "string",
          "pattern": "^#([0-9a-fA-F]{3}|[0-9a-fA-F]{6})$",
          "description": "Override opzionale del colore secondario (badge, `.btn-secondary`). Se assente resta la variante muted di `colorTema`. Se presente sostituisce hue e chroma della catena di derivazione: le varianti light/dark/subtle/emphasis restano comunque calcolate e garantite WCAG.",
          "examples": ["#6c757d"]
        },
        "colorBackground": {
          "type": "string",
          "pattern": "^#([0-9a-fA-F]{3}|[0-9a-fA-F]{6})$",
          "description": "Override opzionale del colore di sfondo (pagina, card, hover, superfici). Se assente resta derivato da `colorTema`. Un solo valore genera automaticamente sia la variante light sia quella dark, come per `colorTema`. Resta comunque una superficie chiara/scura tinta, non un blocco di colore pieno.",
          "examples": ["#f5f6fa"]
        },
        "colorText": {
          "type": "string",
          "pattern": "^#([0-9a-fA-F]{3}|[0-9a-fA-F]{6})$",
          "description": "Override opzionale del colore del testo (corpo e headings). Se assente NON ricade su colorTema ma su colorBackground (che a sua volta è colorTema se nemmeno quello è impostato): testo e sfondo restano sempre intonati tra loro senza sceglierlo esplicitamente. Impostato: override pieno e indipendente. Un solo valore genera automaticamente sia la variante per sfondo chiaro sia quella per sfondo scuro.",
          "examples": ["#1a1a2e"]
        },
        "colorInfo": {
          "type": "string",
          "pattern": "^#([0-9a-fA-F]{3}|[0-9a-fA-F]{6})$",
          "description": "Override opzionale del colore informativo (badge/alert '.text-bg-info', '.alert-info', '.btn-outline-info'). A differenza di colorSecondary/colorBackground/colorText NON ha un fallback derivato dal brand: se assente, Bootstrap 5.3 gestisce '--bs-info*' per intero coi suoi blocchi tono-adattivi nativi. warning/success/danger restano sempre fissi (significato universale, non personalizzabile).",
          "examples": ["#0dcaf0"]
        },
        "smoke": {
          "type": "object",
          "description": "Effetto particellare di sfondo. Ometti o enable:false per disattivarlo.",
          "additionalProperties": false,
          "properties": {
            "enable": { "type": "boolean" },
            "color": { "type": "string", "pattern": "^#([0-9a-fA-F]{3}|[0-9a-fA-F]{6}|[0-9a-fA-F]{8})$" },
            "opacity": { "type": "number", "minimum": 0, "maximum": 1 },
            "maximumVelocity": { "type": "number", "minimum": 0 },
            "particleRadius": { "type": "number", "minimum": 0 },
            "density": { "type": "number", "minimum": 0 }
          }
        }
      }
    },
    "Custom": {
      "type": "object",
      "description": "Valori aggiuntivi liberi, leggibili da entrambi i progetti senza modificare script o codice infrastrutturale.\n\n- Backend (ASP.NET Core): ogni chiave è disponibile tramite IConfiguration[\"Custom:TuaChiave\"] o IConfiguration[\"Custom:Sezione:SottoChiave\"]. Supporta oggetti annidati arbitrari.\n- Frontend Node SSR: disponibile tramite getBr1Settings().Custom in server-env.ts.\n- Browser Angular: disponibile tramite inject(APP_CUSTOM) (l'SSR la serializza in TransferState e il browser la rilegge in idratazione). NON metterci segreti: è committabile e ora visibile al client.\n\nEsempio:\n  \"Custom\": {\n    \"FeatureFlags\": { \"NuovaFunzione\": true },\n    \"Analytics\": { \"TrackingId\": \"UA-XXXXX\" },\n    \"MaxUploadMb\": 10\n  }",
      "additionalProperties": true
    }
  }
}
