# Espone il backend verso l'host - da usare solo se necessario.
# Uso: docker compose -f docker-compose.yml -f docker-compose.backend-exposed.yml up -d --build
services:
  backend:
    ports:
      - "${BACKEND_PORT:-8080}:8080"
